import React from "react";
import CheckField from "./CheckField";

const Main: React.FC = () => {
  return (
    <div className="App">
      {/* CheckFieldコンポーネントを呼び出し */}
      <CheckField />
    </div>
  );
};

export default Main;
